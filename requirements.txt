pip>=24.3.1
setuptools>=75.6.0
numpy==1.24.4  # Required only because of elasticsearch[async]==7.10.1
pydantic==2.13.5  # 2.11.7, 2.10.3 — bumped: pydantic-ai 2.31.0 requires pydantic>=2.12
aiohttp
redis==5.2.1
aioredis==2.0.1
tzlocal
python-multipart # Moze nie byc potrzebne
lark==1.1.2
git+https://github.com/Tracardi/dotty_dict@master
dateparser
pytz
pytimeparse
jsonschema>=4.20.0
python-dateutil
email-validator
urllib3>=1.26,<2  # Moze nie byc potrzebne
geoip2~=4.2.0
aiomysql
PyYAML>=6.0
jinja2
python-dotenv
msgpack==1.0.5
user-agents==2.2.0
SQLAlchemy==2.0.36

# Used by sqlite + sqlachemy
aiosqlite==0.21.0

# Summarizer AI

# pydantic-ai 2.32.0+ requires openai>=3.0.0,
# while instructor currently requires openai<3.0.0.
# Therefore keep pydantic-ai below 2.32.0 while both packages
# are installed in the same environment.

instructor==1.16.0 # Pined to this version as next can require other openai version. eg.>2.45.0.
pydantic-ai==2.31.1
openai==2.54.0  # must stay <3.0.0 for instructor; pydantic-ai 2.31.0 only needs >=2.45.0

# Worker requirements
requests>=2.0.0,<3

# License
cryptography

# System
durable-dot-dict>=0.0.24
kafka-python==2.3.0  # old 2.0.2, 2.1.5
confluent-kafka==2.13.0
duckdb==1.4.1
pyarrow==21.0.0
pandas==2.2.3
dotdict_parser>=0.7.2
qdrant-client
prometheus-client==0.24.0
scikit-learn
